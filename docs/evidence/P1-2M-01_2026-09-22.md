# P1-2M-01 — missed near occurrence / cold fallback recovery

PROMPT_VERSION=P0R (P1 single-final-write scheduler path)
PRIMARY_VARIABLE=lead time (+2m)
WRITES_PER_WAKE=1
INTENDED_DUE=2026-09-22 12:32:00 KST
OBSERVED_RECOVERY_INVOCATION=2026-09-22 13:34:56 KST (automation run context)
WRITE_OK=YES (prior final scheduler update accepted)
STATE_OK=YES (current run context preserved recurring RRULE and enabled automation)
WAKE_OK=NO for intended near occurrence; no turn occurred around 12:32 before the hourly recurrence
WORK_OK=YES on recovered recurrence; canonical GitHub research state was restored and substantive evaluation resumed
DUPLICATE_EXECUTION=NO observed
RECOVERY=PASS via preserved hourly RRULE cold fallback
RESULT=MISSED_NEAR_OCCURRENCE + RECOVERY_PASS

Interpretation:
- +2m is the first tested lead-time condition to fail the intended fast continuation.
- The recurring RRULE did what the architecture requires as a cold fallback: the workload resumed roughly one recurrence later without a new automation.
- This is useful adverse evidence for E5, but one failed +2m sample is insufficient to estimate a stable boundary. Repeat +2m with all other variables fixed before deciding whether +3m is the minimum candidate.
- Runtime timestamps have previously shown early/late anomalies, so exact dispatch-delay estimation remains non-authoritative; the key observation here is the absence of an intervening substantive turn and subsequent hourly recovery.

NEXT_DISCRIMINATING_TEST=P1-2M-02
